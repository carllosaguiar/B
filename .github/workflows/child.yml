name: 'Submodule Notify Parent'

on:
  push:
    branches: [ main, dev, testing ]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

jobs:
  notify:
    name: 'Submodule Notify Parent'
    runs-on: ubuntu-latest

    # Use the Bash shell regardless whether the GitHub Actions runner is ubuntu-latest, macos-latest, or windows-latest
    defaults:
      run:
        shell: bash

    steps:
    - name: Github REST API Call
      env:
        PARENT_REPO: carllosaguiar/A
<<<<<<< HEAD
        WORKFLOW_ID: 18379116
=======
        WORKFLOW_ID: 18332380
>>>>>>> b9700d23b1e687ac55e35532b7245f2d474a057f
      run: |
        curl -X POST -H "Accept: application/vnd.github.v3+json" -H "Authorization: token ${{ secrets.TOKEN_SYNC }}" https://api.github.com/repos/${{ env.PARENT_REPO }}/actions/workflows/${{ env.WORKFLOW_ID }}/dispatches -d '{"ref":"${{ github.ref_name }}"}'